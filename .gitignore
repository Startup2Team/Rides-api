# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Go build output
dist/
tmp/

# Local runtime upload storage — driver docs live in S3/R2, never in git
api-server/var/

# Node tooling clutter (not part of the Go backend)
node_modules/
package.json
package-lock.json

# Test artifacts
coverage.out
coverage.html

# Env files — never commit secrets
api-server/.env

# Agent memory (internal notes, not needed in repo)
.agents/

# Internal AI planning prompts
.prompt.backend.md

# Claude Code local settings
.claude/

# Local tracking files (kept locally, not committed)
LAUNCH_TRACKER.md
api-server/LAUNCH_TRACKER.md
api-server/launch_tracker.json
api-server/generate_tracker.py
api-server/docs/RIDES_WEB_BACKEND_BOOKLOG.md
