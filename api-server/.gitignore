# Compiled binary
dist/
# Stray local `go build` output at the package root (the Docker build uses dist/).
# This was committed by mistake once (a 49 MB ELF binary) and bloated every
# archive/clone/deploy — keep it out for good.
/server

# Environment secrets — NEVER commit
.env
firebase-service-account.json

# Go build artifacts
*.test
*.out
vendor/

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
# Logslogs
*.log
logs/
.cache/
*.log
*.env

# Local pgAdmin dev artifacts (contains a password)
pgadmin-pgpass
pgadmin-servers.json
# Local upload staging
.envvar/uploads/

# Compiled linter binaries
bin/
